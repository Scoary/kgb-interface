<form class="d-flex" method="post" action="{{ path('stash_delete', {'id': stash.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cette donnée ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stash.id) }}">
    <button class="btn btn-sm btn-danger col">Supprimer</button>
</form>